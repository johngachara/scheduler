# Direct dependencies only.
#
# This file was a system-wide `pip freeze`: it carried Brlapi, bcc, brotlipy
# and a TensorFlow toolchain into a service whose entire job is to make three
# HTTP requests on a timer. Celery, its broker client and django-celery-results
# are gone with the scheduler itself -- the server's cron is the scheduler now.
Django>=5.2.6
python-dotenv>=1.2.2
requests>=2.32.5
