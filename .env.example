# The Alltech API this talks to.
ALLTECH_API_URL=https://api.alltechnyeri.co.ke

# Shared machine key, exchanged for a short-lived token. Must match CELERY_KEY
# on the backend.
CELERY_KEY=

# Django needs these even though this service serves no requests.
SECRET_KEY=
DEBUG=False
